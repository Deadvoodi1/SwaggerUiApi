Testing file for init commit

Solution with swagger user API tests

Note:
For generating allure report execute comand in CMD:
allure generate --clean allure-results && allure open allure-report

Btw, this command must fix a problem with allure-report, when it doesnt load in the any browser
